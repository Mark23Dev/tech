from django.apps import AppConfig

class PathsConfig(AppConfig):
    """Configuration for the Paths app."""
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'paths'
    verbose_name = 'Learning Paths'
